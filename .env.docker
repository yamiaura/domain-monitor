# Docker Environment Configuration
# Copy this file to .env and customize as needed

# Application
APP_ENV=production
APP_PORT=8080

# Database
DB_HOST=db
DB_PORT=3306
DB_DATABASE=domain_monitor
DB_USERNAME=domain_monitor
DB_PASSWORD=changeme_secure_password
DB_ROOT_PASSWORD=changeme_root_password

# phpMyAdmin (optional)
PMA_PORT=8081

# Note: APP_ENCRYPTION_KEY will be generated during web installation
# Visit http://localhost:8080/install after starting containers
